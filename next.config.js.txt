/** @type {import('next').NextConfig} */
const nextConfig = {
  // ⭐️ الإعداد الأول: لتصدير المشروع كملفات HTML ثابتة
  output: 'export',
  
  // ⭐️ الإعداد الثاني: لتحديد مسار القاعدة ليتطابق مع اسم المستودع
  // يجب عليك تغيير '/TD7-PORTFOLIO' إلى الاسم الفعلي لمستودعك (أفترض أنه TD7-PORTFOLIO من الصورة)
  basePath: '/TD7-PORTFOLIO', // تأكد من مطابقة الاسم بدقة (مع الشرطة المائلة الأمامية)
  
  // إعداد الصور الذي أضفته
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;