/** @type {import('next').NextConfig} */
const nextConfig = {
  // هذا الإعداد يخبر Next.js بتصدير المشروع كملفات HTML ثابتة في مجلد "out"
  output: 'export',
  
  // قم بإضافة مسار القاعدة (Base Path) ليتطابق مع اسم المستودع (Repository) الخاص بك
  // يجب عليك تغيير 'YOUR_REPO_NAME' إلى الاسم الفعلي لمستودعك على GitHub
  // مثال: إذا كان رابطك هو: https://github.com/myuser/my-portfolio، فاستبدلها بـ '/my-portfolio'
  basePath: '/YOUR_REPO_NAME',
  
  // إعداد اختياري: لتعطيل استخدام الصور المحسّنة التي قد لا تعمل في التصدير الثابت
  images: {
    unoptimized: true,
  }
};

module.exports = nextConfig;